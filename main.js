 function countSymbols (n) {
 	var result;
 	var str = n.toString();
 	for (var i=count=0; i<str.length; count+=+(stringsearch===str[i++])
 		if (char == char.toUpperCase()) {
        upper++;
    }
    else if (char == char.toLowerCase()) {
        lower++;
    }
    else if (!isNaN(char)) {
        digits++;
    }
    else {
        symbols++;
    }

    return result;
 }